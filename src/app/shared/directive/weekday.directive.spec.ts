import { WeekdayDirective } from './weekday.directive';

describe('WeekdayDirective', () => {
  it('should create an instance', () => {
    const directive = new WeekdayDirective();
    expect(directive).toBeTruthy();
  });
});
